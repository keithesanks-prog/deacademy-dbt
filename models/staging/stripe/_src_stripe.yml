version: 2

sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payment
        # Ensure 'freshness' is at the same level as 'name' for standard parsing
        loaded_at_field: _BATCHED_AT
        # freshness:
        #   warn_after: {count: 12, period: hour}
        #   error_after: {count: 60, period: day}